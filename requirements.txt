# Chintu AI - Python dependencies (Windows)
#
# Notes:
# - Keep this list focused on runtime needs. Some heavy ML deps (torch, etc.)
#   are pulled transitively (e.g., via sentence-transformers).
# - If you only want the UI + basic tools, you can still run without some
#   optional capabilities. The launcher scripts will warn when something is
#   missing.

pydantic>=2.0,<3.0
pydantic-settings>=2.5.2,<3.0
websockets>=11.0
requests>=2.31.0
beautifulsoup4>=4.12.0
readability-lxml>=0.8.1
ddgs>=6.0.0
psutil>=5.9.0
pyautogui>=0.9.54
Pillow>=10.0.0

# Browser automation
playwright>=1.40.0

# API / gateway + MCP transport
fastapi>=0.135.1
uvicorn>=0.41.0
mcp>=1.26.0

# Media / content
youtube-transcript-api>=1.2.0
edge-tts>=6.1.0

# Windows automation
uiautomation>=2.0.0

# Memory / embeddings
chromadb>=0.5.0
sentence-transformers>=2.2.0
numpy>=1.26.0

# Audio (optional but recommended for voice)
pyaudio>=0.2.13
sounddevice>=0.4.6
faster-whisper>=1.0.0

# Integrations
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.2.0
google-auth>=2.0.0
python-telegram-bot>=22.0,<23.0

# Security / vault
keyring>=25.0.0
cryptography>=42.0.0

# Dev / tests
pytest>=8.0.0
