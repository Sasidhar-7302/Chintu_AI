# Legacy Script Archive

This folder contains one-off diagnostic and validation scripts that are not part of the active operational toolchain.

Rules:
- Do not call these scripts from production workflows.
- Keep active scripts in `scripts/` root only.
- If a legacy script is needed again, promote it back to `scripts/` with tests and docs updates.

Archived from `scripts/` during repository hygiene pass on 2026-02-24.

